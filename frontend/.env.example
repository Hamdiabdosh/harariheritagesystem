# Frontend talks to the Go API (must match backend PORT + /api/v1 prefix)
# Local dev
VITE_API_URL=http://localhost:8080/api/v1
# Production (Coolify) — use the API subdomain, not the frontend domain:
# VITE_API_URL=https://api.heritage.raafat.site/api/v1
